
import React from 'react';
import { OutfitSuggestion } from '../types';

export const ComparisonView = ({ outfits, onClose }: { outfits: OutfitSuggestion[], onClose: () => void }) => {
  return null; // Implemented via Canvas logic in App.tsx
};
